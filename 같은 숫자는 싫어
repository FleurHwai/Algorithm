def solution(arr):
    answer = []
    for i,num in enumerate(arr):
        if i==0:
            answer.append(num)
        else:
            if num==arr[i-1]:
                pass
            else:
                answer.append(num)
    return(answer)
