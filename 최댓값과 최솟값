def solution(s):
    answer = ''
    num=s.split(" ")
    numbers=[]
    for i in range(len(num)):
        numbers.append(int(num[i]))
    answer+=(str(min(numbers)))
    answer+=" "
    answer+=(str(max(numbers)))
    return answer
